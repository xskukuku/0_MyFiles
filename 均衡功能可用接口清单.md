# BUS30 BCU 均衡功能可用接口清单

## 1. 电压相关接口

### 1.1 单体电压最大值
- `RteGetTotalCellVoltMaxMax(void)` - 获取全局单体电压最大值（所有pack中的最大值）
- `RteGetTotalCellVoltMax(uint8 index)` - 获取全局第index个最大单体电压
- `RteGetCellVoltMax(uint8 pack, uint8 index)` - 获取指定pack的第index个最大单体电压
- `RteGetTotalCellVoltMaxModule(uint8 index)` - 获取最大电压所在的模块号
- `RteGetTotalCellVoltMaxNum(uint8 index)` - 获取最大电压的单体编号
- `RteGetTotalCellVoltMaxPos(uint8 index)` - 获取最大电压的位置

### 1.2 单体电压最小值
- `RteGetTotalCellVoltMinMin(void)` - 获取全局单体电压最小值（所有pack中的最小值）
- `RteGetTotalCellVoltMin(uint8 index)` - 获取全局第index个最小单体电压
- `RteGetCellVoltMin(uint8 pack, uint8 index)` - 获取指定pack的第index个最小单体电压
- `RteGetTotalCellVoltMinModule(uint8 index)` - 获取最小电压所在的模块号
- `RteGetTotalCellVoltMinNum(uint8 index)` - 获取最小电压的单体编号
- `RteGetTotalCellVoltMinPos(uint8 index)` - 获取最小电压的位置

### 1.3 电压压差
- `RteGetTotalCellVoltDiff(void)` - 获取全局单体电压压差（最大值-最小值）
- `RteGetCellVoltDiff(uint8 pack)` - 获取指定pack的单体电压压差

### 1.4 电压平均值和总和
- `RteGetTotalCellVoltMean(void)` - 获取全局单体电压平均值
- `RteGetCellVoltMean(uint8 pack)` - 获取指定pack的单体电压平均值
- `RteGetTotalCellVoltSum(void)` - 获取全局单体电压总和
- `RteGetCellVoltSum(uint8 pack)` - 获取指定pack的单体电压总和

### 1.5 单体电压获取
- `RteGetCellVoltage(uint8 pack, uint16 index)` - 获取指定pack的第index个单体电压值

### 1.6 系统电压
- `RteGetPackBusVolt(void)` - 获取母线电压
- `RteGetLinkPosVolt(void)` - 获取正极连接电压
- `RteGetLinkNegVolt(void)` - 获取负极连接电压

### 1.7 电压有效性
- `RteGetTotalCellVoltMaxMaxValidity(void)` - 获取全局最大电压有效性
- `RteGetTotalCellVoltMinMinValidity(void)` - 获取全局最小电压有效性

## 2. 温度相关接口

### 2.1 单体温度最大值
- `RteGetTotalCellTempMaxMax(void)` - 获取全局单体温度最大值（所有pack中的最大值）
- `RteGetTotalCellTempMax(uint8 index)` - 获取全局第index个最大单体温度
- `RteGetCellTempMax(uint8 pack, uint8 index)` - 获取指定pack的第index个最大单体温度
- `RteGetTotalCellTempMaxModule(uint8 index)` - 获取最大温度所在的模块号
- `RteGetTotalCellTempMaxNum(uint8 index)` - 获取最大温度的探头编号
- `RteGetTotalCellTempMaxPos(uint8 index)` - 获取最大温度的位置

### 2.2 单体温度最小值
- `RteGetTotalCellTempMinMin(void)` - 获取全局单体温度最小值（所有pack中的最小值）
- `RteGetTotalCellTempMin(uint8 index)` - 获取全局第index个最小单体温度
- `RteGetCellTempMin(uint8 pack, uint8 index)` - 获取指定pack的第index个最小单体温度
- `RteGetTotalCellTempMinModule(uint8 index)` - 获取最小温度所在的模块号
- `RteGetTotalCellTempMinNum(uint8 index)` - 获取最小温度的探头编号
- `RteGetTotalCellTempMinPos(uint8 index)` - 获取最小温度的位置

### 2.3 温度温差
- `RteGetTotalCellTempDiff(void)` - 获取全局单体温度温差（最大值-最小值）
- `RteGetCellTempDiff(uint8 pack)` - 获取指定pack的单体温度温差

### 2.4 温度平均值
- `RteGetTotalCellTempMean(void)` - 获取全局单体温度平均值
- `RteGetCellTempMean(uint8 pack)` - 获取指定pack的单体温度平均值

### 2.5 单体温度获取
- `RteGetCellTemp(uint8 pack, uint16 index)` - 获取指定pack的第index个温度探头值

### 2.6 温度有效性
- `RteGetTotalCellTempMaxMaxValidity(void)` - 获取全局最大温度有效性
- `RteGetTotalCellTempMinMinValidity(void)` - 获取全局最小温度有效性

## 3. SOC相关接口

### 3.1 SOC获取
- `RteGetTotalSoc(void)` - 获取全局显示SOC
- `RteGetTotalRealSoc(void)` - 获取全局真实SOC
- `RteGetTotalIntegrateSoc(void)` - 获取全局积分SOC
- `RteGetTotalTemperSoc(void)` - 获取全局温度修正SOC
- `RteGetSoc(uint8 pack)` - 获取指定pack的显示SOC
- `RteGetRealSoc(uint8 pack)` - 获取指定pack的真实SOC
- `RteGetIntegrateSoc(uint8 pack)` - 获取指定pack的积分SOC

### 3.2 SOC最大最小值
- `RteGetTotalSocMax(void)` - 获取全局SOC最大值
- `RteGetTotalSocMin(void)` - 获取全局SOC最小值

### 3.3 容量相关
- `RteGetTotalRemindCap(void)` - 获取剩余容量
- `RteGetTotalRealCap(void)` - 获取真实容量
- `RteGetTotalRemained_Engy(void)` - 获取剩余能量

## 4. 电流相关接口

### 4.1 电流获取
- `RteGetTotalCurrentValue(void)` - 获取总电流值
- `RteGetCurrentValue(uint8 pack)` - 获取指定pack的电流值
- `RteGetTotalCurr100msMean(void)` - 获取100ms电流平均值
- `RteGetTotalStmCurrMean(void)` - 获取短期电流平均值
- `RteGetTotalLtmCurrMean(void)` - 获取长期电流平均值

### 4.2 电流限制（SOP功率限制）
- `RteGetTotalDchCurrLimit(void)` - 获取放电持续电流限制
- `RteGetTotalChgCurrLimit(void)` - 获取充电持续电流限制
- `RteGetTotalDchPeakCurrLimit(void)` - 获取放电峰值电流限制
- `RteGetTotalChgPeakCurrLimit(void)` - 获取充电峰值电流限制
- `RteGetTotalDchPeakCurrLimit10S(void)` - 获取10s放电峰值电流限制
- `RteGetTotalChgPeakCurrLimit10S(void)` - 获取10s充电峰值电流限制
- `RteGetTotalDchPeakCurrLimit30S(void)` - 获取30s放电峰值电流限制
- `RteGetTotalChgPeakCurrLimit30S(void)` - 获取30s充电峰值电流限制

## 5. OCV（开路电压）相关表格

### 5.1 OCV温度表
- `gucarrOcvTempForm[AMEND_OCV_TEMP_DEMESION]` - OCV温度点数组
  - 通常包含：-10°C, 0°C, 10°C, 25°C, 35°C

### 5.2 OCV电压-SOC映射表
- `guarrOcvForm[AMEND_OCV_TEMP_DEMESION][AMEND_OCV_SOC_DEMESION]` - OCV电压表（二维数组）
  - 第一维：温度点（5个）
  - 第二维：SOC点（6个，通常是0%, 5%, 10%, 15%, 20%, 25%）

### 5.3 OCV对应的SOC点
- `guarrOcvSoc[AMEND_OCV_SOC_DEMESION]` - OCV对应的SOC点数组
  - 通常包含：0, 50, 100, 150, 200, 250（对应0%, 5%, 10%, 15%, 20%, 25%）

## 6. 电池单体配置参数

### 6.1 单体电压限制
- `glbuCellPlsbleMinVolt` - 单体最低可用电压
- `glbuCellPlsbleMaxVolt` - 单体最高可用电压
- `glbuCellDCHMinVolt` - 单体放电截止电压
- `glbuCellCHGMaxVolt` - 单体充电截止电压
- `glbuCellRatedVolt` - 单体额定电压
- `glbuCellCHGStage1Volt` - 阶段1充电电压
- `glbuCellCHGStage2Volt` - 阶段2充电电压

### 6.2 SOC修正电压点
- `glbuCellFulAmendVolt` - 满电修正电压
- `glbuCellFulAmendMeanVolt` - 满电修正平均电压
- `glbuCellEmptAmendVolt` - 空电修正电压
- `glbuCellSOC5AmendVolt` - SOC 5%修正电压
- `glbuCellSOC10AmendVolt` - SOC 10%修正电压
- `glbuCellSOC15AmendVolt` - SOC 15%修正电压
- `glbuCellSOC20AmendVolt` - SOC 20%修正电压

## 7. 系统配置参数

### 7.1 电池系统配置
- `glbucSysCellParallel` - 单体并联数
- `glbucSysPackParallel` - Pack并联数
- `glbucSysVoltSeries` - 电压串联数（总单体数）
- `glbucSysTempSeries` - 温度串联数（总温度探头数）
- `glbucSysBmuCount` - BMU数量
- `glbuSysTotalCellNum` - 总单体数量
- `glbuSysTotalTempNum` - 总温度探头数量

### 7.2 容量参数
- `glbuCellCap` - 单体容量
- `glbuBranchCapacity` - 支路容量
- `glbuSysCapacity` - 系统容量
- `glbuSysRatedVolt` - 系统额定电压
- `glbuSysRatedEngy` - 系统额定能量

## 8. 均衡控制接口

### 8.1 均衡功能API
- `AlgInitBalance(void)` - 初始化均衡功能
- `AlgTaskBalance(void)` - 均衡任务主函数
- `ApiClearBalanceFlag(void)` - 清除均衡标志
- `ApiSetBalanceFlag(void)` - 设置均衡标志
- `ApiSetLowSOCBalanceFlag(void)` - 设置低SOC均衡标志
- `ApiSetForceBalance(void)` - 设置强制均衡
- `ApiClearForceBalance(void)` - 清除强制均衡
- `RteGetBalanceActiveSt(void)` - 获取均衡激活状态

## 9. 故障相关接口

### 9.1 电压故障
- `RteGetCellVoltHighErr(void)` - 单体过压故障
- `RteGetCellVoltLowErr(void)` - 单体欠压故障
- `RteGetCellVoltDiffErr(void)` - 单体压差故障
- `RteGetCellVoltExtremeLowErr(void)` - 单体极低压故障

### 9.2 温度故障
- `RteGetCellTemperHighErr(void)` - 单体过温故障
- `RteGetCellTemperLowErr(void)` - 单体低温故障
- `RteGetCellTemperDiffErr(void)` - 单体温差故障

### 9.3 SOC故障
- `RteGetSocLowErr(void)` - SOC过低故障
- `RteGetSocHighErr(void)` - SOC过高故障
- `RteGetSocDiffErr(void)` - SOC差异故障
- `RteGetSocDiscontinueErr(void)` - SOC不连续故障

### 9.4 总体故障等级
- `RteGetTotalFaultLevel(void)` - 获取总体故障等级
- `RteGetTotalStandbyFaultLevel(void)` - 获取总体待机故障等级

## 10. SOH（健康状态）接口

- `RteGetTotalSohValue(void)` - 获取总体SOH值

## 11. SOF（功能状态）接口

- `RteGetSocDiscontinueValue(void)` - 获取SOC不连续值
- `RteGetTempRiseValue(void)` - 获取温升值
- `RteGetBmuTempRiseValue(uint8 bmuindex)` - 获取指定BMU的温升值
- `RteGetTempDiffRiseValue(void)` - 获取温差上升值

## 使用建议

### 均衡策略编写时的常用组合：

1. **基于压差的均衡**
   - 使用 `RteGetTotalCellVoltMaxMax()` 和 `RteGetTotalCellVoltMinMin()` 获取最大最小电压
   - 使用 `RteGetTotalCellVoltDiff()` 获取压差
   - 使用 `RteGetTotalCellVoltMaxModule()` 和相关接口定位需要均衡的单体

2. **基于SOC的均衡**
   - 使用 `RteGetTotalSocMax()` 和 `RteGetTotalSocMin()` 获取SOC差异
   - 结合OCV表格 `guarrOcvForm` 和 `gucarrOcvTempForm` 进行SOC估算

3. **考虑温度的均衡**
   - 使用 `RteGetTotalCellTempMaxMax()` 和 `RteGetTotalCellTempMinMin()` 监控温度
   - 使用 `RteGetTotalCellTempDiff()` 判断温差是否过大
   - 温度过高时可能需要停止或降低均衡强度

4. **充电状态判断**
   - 使用 `RteGetTotalCurrentValue()` 判断充放电状态
   - 充电结束后是启动均衡的最佳时机

5. **安全边界检查**
   - 使用 `glbuCellPlsbleMinVolt` 和 `glbuCellPlsbleMaxVolt` 确保单体电压在安全范围内
   - 使用故障接口确保无严重故障时才进行均衡

## 注意事项

1. 所有电压单位通常为 **mV**（毫伏）
2. 所有温度单位通常为 **°C**，可能带有偏移量（如+40°C）
3. SOC单位通常为 **0.1%**（即1000表示100%）
4. 电流单位通常为 **0.1A**，偏移量为30000（30000表示0A）
5. `index` 参数通常从0开始，用于获取多个最值（如前3个最大值）
6. `pack` 参数表示电池包号，范围取决于系统配置

## 文件位置参考

- 接口声明：`src/service/bmu/bmu.h`
- 全局定义：`src/SysCfg/Global_define.h`
- 均衡功能：`src/alg/balance/balance.h` 和 `balance.c`
- SOC算法：`src/alg/soc/soc.h` 和 `soc.c`
- SOP功率：`src/alg/sop/sop.h` 和 `sop.c`
